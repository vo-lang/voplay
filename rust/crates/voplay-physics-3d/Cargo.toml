[package]
name = "voplay-physics-3d"
version = "0.1.0"
edition = "2021"
description = "Voplay deterministic 3D physics artifact"

[dependencies]
voplay-runtime = { path = "../voplay-runtime", default-features = false, features = ["core", "physics3d"] }
